# Configuration for probot-stale - https://github.com/probot/stale

# Number of days of inactivity before an Issue or Pull Request becomes stale
daysUntilStale: 60

# Number of days of inactivity before an Issue or Pull Request with the stale label is closed.
# Set to false to disable. If disabled, issues still need to be closed manually, but will remain marked as stale.
daysUntilClose: 7

# Only issues or pull requests with all of these labels are check if stale. Defaults to `[]` (disabled)
onlyLabels: []

# Issues or Pull Requests with these labels will never be considered stale. Set to `[]` to disable
exemptLabels:
  - pinned
  - security

# Set to true to ignore issues in a project (defaults to false)
exemptProjects: false

# Set to true to ignore issues in a milestone (defaults to false)
exemptMilestones: false

# Set to true to ignore issues with an assignee (defaults to false)
exemptAssignees: false

# Label to use when marking as stale
staleLabel: probot-stale

# Comment to post when marking as stale. Set to `false` to disable
markComment: >
  Esse problema foi marcado automaticamente como obsoleto porque não teve
  atividade recente. Será fechado se nenhuma atividade adicional ocorrer.

# Comment to post when removing the stale label.
unmarkComment: >
  Esse problema foi desmarcado como obsoleto, e não será removido automaticamente.

# Comment to post when closing a stale Issue or Pull Request.
closeComment: >
  Esse problema está sendo encerrado por inatividade. Obrigado pela contribuição!

# Limit the number of actions per hour, from 1-30. Default is 30
limitPerRun: 30
